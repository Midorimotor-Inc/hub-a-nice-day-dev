# GAS v12 再デプロイ手順書（書き込み高速化）

対象: Kyoshi（GASプロジェクト「Hub a Nice Day データ」の編集者）
作成: 2026-08-25 / 想定所要: 5分 / **フロント側の変更は不要**

## これは何か

**書き込みが「データ量に関係なく1本10〜27秒」かかっていた原因を潰す。**

実測（DEV・数バイトの値）:

| | 時間 |
|---|---|
| 読み取り1回 | 1.1秒 |
| 書き込み1回 | 4.8 / 11.4 / 8.2秒 |

数バイトなのに書き込みだけ4〜11秒＝**遅さの正体はネットワークでもデータ量でもなく、GASの書き込み処理そのもの**だった。コードを読んで無駄が3つ見つかったので削る。

1. `invalidateCache` が `cache.remove()` を **1+100回ループ**していた（CacheServiceは1回ごとにリモート往復）→ `removeAll` で1往復に
2. `writeRow` が値と更新日時を **`setValue`×2** で書いていた → `setValues` 1回に
3. 書き込みのたびに `findRow` と `deleteChunks` で **キー列(A列)を2回フルスキャン**していた → 1回に集約し、`__chunk` 行が無ければシートに触らない（v9でDrive保管に移った後は通常0行）

さらに **`action=setMany`（複数キーを1リクエスト・1回のロックで書く／最大20件）を追加**した。1件の予約が insp / lres / custbk と複数キーに散るため、1本ずつロックを取り直すのをやめられる。**ただし今回はフロントは使わない**（`caps.setMany` を見てオプトインする作りにしてある）。効果を測ってから切り替える。

## 安全性

- 既存の読み取り経路・snapshot系・patchInsp・notifyFail は**無変更**。
- 変更した書き込みロジックは**モックのスプレッドシートで単体テスト17件PASS**（`node gas_v12_test.js`）。
  テスト中に「setManyで旧chunk行を消すと行番号がずれ、次のキーを別の行に書く」バグを1件検出・修正済み。
- **切り戻しはデプロイ画面から1つ前のバージョンを選ぶだけ**（URL・デプロイIDは変わらない）。

## やること

1. GASエディタを開く（**会社アカウント `hubaniceday.system@gmail.com` でログインしていること**）
2. `GAS_server_v10_snapshots.gs` を**メモ帳で開いて全選択コピー**
   （チャットから貼ると全角化けの恐れ。必ずファイルから）
3. GASエディタの「コード.gs」を全選択して貼り替え → 保存（Ctrl+S）
4. 右上「デプロイ」→「デプロイを管理」→ 鉛筆(編集) → バージョン「**新バージョン**」→「デプロイ」
   - **デプロイID・URLは変わらない。トリガー再登録は不要。**
5. 確認: ブラウザで
   `<GAS_URL>?action=caps&apiKey=hub2026co-f466kt5vs3vnDQDPuwWeS6XM`
   を開き、`{"patchInsp":true,"notifyFail":true,"snapshots":true,"setMany":true,"ver":"v12"}` が返ればOK
   （**`"ver":"v11"` のままなら新バージョンとしてデプロイできていない**）

## デプロイ後

Claude側で書き込み時間を再計測する（デプロイ前は 4.8〜11.4秒）。
測って初めて「どれだけ速くなったか」が言える。想定どおり縮んでいれば、
次に `setMany` を使うフロント側のオプトイン（第2段）に進む。

縮んでいなければデプロイ画面から前バージョンへ戻す。
