# GAS v13 再デプロイ手順書（代車が消えて復活する現象の根本対策）

対象: Kyoshi / 想定所要: 5分 / **フロント側の変更は不要**

## これは何か

**読み取りキャッシュの寿命を 60秒 → 5秒 に短縮する。それだけ。**

### なぜ必要か（2026-08-26の実測）

代車を1件付けただけで、代車データの書き込みが**3回も再送**されていた。
フロントは「書く → 読み返して確認 → 入っていなければ再送」という手順で保存の
着地を保証しているが、その**読み返しが古い値を返す**ため確認に失敗している。

原因はGASの読み取りキャッシュ。書き込み時に `invalidateCache` でキャッシュを消して
いるが、**書き込みの「前」に始まった読み取りが、書き込みの「後」にキャッシュへ
古い値を書き戻す**ことがある。こうなると、その後の読み取りは最大60秒のあいだ
「代車が入っていない古い値」を返し続ける。

画面で起きること:

```
代車を付ける → 表示される
           → 古い値を読んだ処理が走る → 代車が消える
           → キャッシュが切れる     → 復活する
```

ユーザー報告「代車が一旦消えて数秒後に復活する」「入れ直したら直った」は、
すべてこの形で説明がつく。**クライアント側で何重に防いでも、サーバーが古い値を
返し続ける限り揺れは止まらない。**

読み取り自体は1秒前後で終わるので、キャッシュを短くしても体感は変わらない。

## 安全性

- 変更は定数1つ（`READ_CACHE_SEC = 5`）と、それを使う3か所だけ。ロジックは無変更。
- 既存の読み取り・書き込み・snapshot系・patchInsp・setMany は一切変えていない。
- 書き込みロジックのモック単体テスト17件は引き続きPASS（`node gas_v12_test.js`）。
- **切り戻しはデプロイ画面で1つ前のバージョンを選ぶだけ**（URL・デプロイIDは不変）。

## やること

1. `GAS_server_v10_snapshots.gs` を**メモ帳で開く**（チャットから貼ると文字化けします）
   場所: `C:\Users\A\HUB-A-NICE-DAY-DEV\GAS_server_v10_snapshots.gs`
2. GASエディタを開く（**会社アカウント `hubaniceday.system@gmail.com` でログイン**）
   https://script.google.com/u/3/home/projects/1Y6RxxhMr-s-6ja6zJekKgUsFnpPhg8HGYDaiBuEzw-tduThaGuEGhsTo/edit
3. メモ帳で **Ctrl+A → Ctrl+C**
4. GASエディタの「コード.gs」で **Ctrl+A → Ctrl+V → Ctrl+S**
5. 右上「デプロイ」→「**デプロイを管理**」→ 鉛筆（編集）→ バージョン「**新バージョン**」→「デプロイ」
   - 「新しいデプロイ」は押さないこと（URLが変わって繋がらなくなる）

## 確認

ブラウザで開いて `"ver":"v13"` になっていればOK:

```
<GAS_URL>?action=caps&apiKey=hub2026co-f466kt5vs3vnDQDPuwWeS6XM
```

`"ver":"v12"` のままなら、手順5の「新バージョン」ができていない。

## デプロイ後に確認したいこと

代車を1件付けて、**書き込みの再送が起きないこと**（＝1回で着地すること）。
再送が消えれば、画面の揺れも止まるはず。
