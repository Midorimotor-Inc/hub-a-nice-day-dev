# GAS v14 再デプロイ手順書（利用者認証）

対象: Kyoshi / 想定所要: 10分 / **この時点ではフロント側の変更は不要・誰も影響を受けません**

## これは何か

**「URLとAPIキーさえ知っていれば誰でも読み書きできる」状態を塞ぐための土台。**

リポジトリが公開なので、HTMLの中のAPIキーは誰でも読めます。つまり今は、URLを知った人が
予約を消すことも書き換えることもできます。これを「APIキー **＋ 利用証**」の2つが揃った
要求だけ通すようにします。利用証はGASだけが持つ秘密鍵で署名するので、偽造できません。

**今回のデプロイでは、まだ何も必須になりません。** 仕組みを入れるだけです。
必須にするのは、全員の登録が済んだあとに**スイッチを入れる**別作業（この文書の後半）。

## 事前に知っておくこと

- **DEVと本番は同じGASを共有しています。** そのためスイッチは環境ごとに分けてあります
  （`HUB_AUTH_ENFORCE_DEV` と `HUB_AUTH_ENFORCE`）。DEVだけで先に試せます。
- **メール送信の権限**が要ります。すでに `notifyFail` で MailApp を使っているので、
  以前に承認済みなら追加の承認は出ないはずです。出たら承認してください。
- 貼り付けるコードは **`GAS_server_v14_auth.gs`**。これは
  `node build_gas_v14.js` が機械的に生成したもので、既存988行には手を触れていません。
  **手で編集しないでください。**

---

## 手順

### 1. GASプロジェクトを開く

**会社アカウント `hubaniceday.system@gmail.com` でログインしていることを必ず確認**してください。

過去に、個人アカウント（`ega.turbo…`）でログインした窓を開いてしまい、**旧環境のプロジェクトを
編集しかけた事故**があります。プロジェクトを開いたら**最終更新日**を見て、想定どおりか確認します。

- プロジェクトID: `1Y6RxxhMr-s-6ja6zJekKgUsFnpPhg8HGYDaiBuEzw-tduThaGuEGhsTo`
- フロントが使っているデプロイID: `AKfycbyN…` で始まるもの

### 2. 今のコードを退避する（切り戻し用）

エディタのコードを**全選択してコピーし、メモ帳に貼って保存**してください。
ファイル名の例: `GAS_backup_20260901.txt`

**これが唯一の切り戻し手段です。必ず取ってください。**

### 3. 新しいコードを貼り付ける

1. エクスプローラーで `C:\Users\A\HUB-A-NICE-DAY-DEV\GAS_server_v14_auth.gs` を
   **メモ帳で開く**（チャットからコピーすると全角文字が化けて構文エラーになります）
2. **Ctrl+A → Ctrl+C**
3. GASエディタでコードを**全選択（Ctrl+A）→ Ctrl+V** で置き換え
4. **Ctrl+S** で保存

### 4. デプロイする（URLを変えないこと）

**「デプロイ」→「デプロイを管理」→ 稼働中のデプロイ（`AKfycbyN…`）の鉛筆マーク →
バージョン「新バージョン」→「デプロイ」**

> ⚠️ **「新しいデプロイ」を選ばないでください。** URLが変わり、アプリが繋がらなくなります。
> 必ず**既存のデプロイを編集**します。

権限の確認画面が出たら承認してください。

### 5. 動作確認

ブラウザで本番アプリを開き、**予約の表示・保存がこれまでどおり動く**ことを確認します。
この段階では利用証は不要なので、**何も変わらないのが正解**です。

続いてバージョンを確認します。アプリのどこかで開発者ツール（F12）のコンソールに
以下を貼って実行してください。

```
fetch(GAS_URL+'?action=caps&apiKey='+encodeURIComponent(GAS_API_KEY)).then(r=>r.text()).then(console.log)
```

`"ver":"v14"` と `"auth":true` が返れば成功です。

**ここまでで今日の作業は終わりです。** 誰も影響を受けていません。

---

## 後日：スイッチを入れる（必須化）

**全スタッフの登録が済んでから**行います。順番を守ってください。

### 手順A：まずDEVだけで試す

GASエディタ左の**歯車（プロジェクトの設定）→「スクリプト プロパティ」→「スクリプト プロパティを追加」**

| プロパティ | 値 |
|---|---|
| `HUB_AUTH_ENFORCE_DEV` | `1` |

**反映は最大60秒**（判定を60秒キャッシュしているため）。

DEVサイトを開いて、**登録済みの端末では今までどおり使えること**、
**利用証を消した状態では弾かれること**を確認します。

### 手順B：本番も必須にする

DEVで問題が無いことを確認してから、同じ要領で追加します。

| プロパティ | 値 |
|---|---|
| `HUB_AUTH_ENFORCE` | `1` |

**この瞬間から、URLを知っているだけの人は一切操作できなくなります。**

---

## 切り戻し

**症状が出たら、まずスイッチを消してください。** これが最速です。

- スクリプトプロパティから `HUB_AUTH_ENFORCE` / `HUB_AUTH_ENFORCE_DEV` を**削除**
- 最大60秒で、認証を入れる前と同じ動作に戻ります（コードの入れ替えは不要）

コードごと戻したい場合は、手順2で退避したファイルの中身を貼り直し、
手順4と同じ方法で**既存デプロイを編集**して再デプロイします。

---

## 自動で作られるもの（手動設定は不要）

| プロパティ | 用途 |
|---|---|
| `HUB_AUTH_SECRET` | 利用証の署名鍵。初回に自動生成される |

> ⚠️ **この鍵を消したり書き換えたりしないでください。** 発行済みの利用証がすべて無効になり、
> 全員が再ログインを求められます（データは失われません）。

シートには `hub-v8-auth-devices` / `hub-v8-dev-auth-devices` という行が増えます。
これは**登録済み端末の台帳**で、管理画面から一覧・取り消しができるようになります。

---

## 検証済みであること

デプロイ前に、以下を自動テストで確認済みです。

```
node gas_auth_v14_test.js              → 31件 PASS（署名・改ざん検知・期限・取り消し・段階移行）
node gas_v12_test.js                   → 17件 PASS（既存GASの動作）
node gas_v12_test.js GAS_server_v14_auth.gs → 17件 PASS（認証を足しても既存が壊れていない）
```

特に3つ目が重要で、**認証の追加によって既存の動作が1つも変わっていない**ことの証拠です。

---

## v15 への再デプロイ（2026-09-04・招待メールとスライド期限）

`GAS_server_v14_auth.gs` に次の2つが増えました。**これを反映しないと、スタッフ管理の
「招待メールを送る」が失敗し、利用証の期限も延びません**（発行から90日で切れます）。

- `authInvite_` … 管理者が押す招待メール。スタッフ表に登録済みのアドレスにしか送りません。
- `authRenew_`  … 利用証の期限を「最後に使った日から90日」に延ばします。

### 手順

1. `node build_gas_v14.js` を実行して `GAS_server_v14_auth.gs` を作り直す
2. そのファイルを**メモ帳で開いて**全選択・コピー
   （チャットからコピーすると全角文字が化けて構文エラーになります）
3. Apps Script のエディタに貼り付けて保存
4. **デプロイを管理 → 既存のデプロイを編集 → 新バージョン → デプロイ**
   （新規デプロイを作るとURLが変わり、繋がらなくなります）
5. ブラウザで `?action=caps&apiKey=本来のキー` を開き、`ver:"v14"` が返ることを確認
6. スタッフ管理で自分に招待メールを送り、実際に届くか1通で確かめる

### 確認しておくこと

- スクリプトプロパティ `HUB_AUTH_ENFORCE_DEV` / `HUB_AUTH_ENFORCE` は**まだ設定しない**。
  全員の端末登録が済んでから `1` にします（判定は60秒キャッシュ）。
- 招待メールに載るURLは `AUTH_APP_URL`（モジュール冒頭）に書いてあります。
  Pages のURLを変えた時はここも直すこと。
